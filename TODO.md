# TODO

## Plan de fixes

- [ ] Corregir `deleteInvoice` en `app/lib/actions.ts` (remover `throw` inalcanzable).
- [ ] Corregir componente de login: agregar `'use client';` en `app/ui/login-form.tsx` para que funcionen `useActionState` y `useSearchParams`.
- [x] Ejecutar `pnpm lint`.
- [x] Ejecutar `pnpm run build`.

- [ ] Probar manualmente: Login y eliminación de invoice.
